giGIT

gin clone и вставл€ем ссылку с гитхаба - копирует папку на комп из репозитори€ гитхаба

git init (1 раз, создает папку .git)
git add . (отслеживать все файлы)
git commit -m "comment" («апомнить текущее состо€ние сайта и добавить коммент)
git push origin название ветки (git push origin master) - закачать файлы на удаленный репозиторий (github)

git branch branchName - создать новую ветку
git checkout branchName - сменить ветку
git branch -v - посмотреть ветки
git merge название ветки - сли€ние веток (делаетс€ из мастера потом пушитс€)

GitHub
—оздать репозиторий
—в€зать с папкой на компе (ѕункт Еor push an existing repository from the command line)

git rm -r название - удал€ет каталог - затем коммитим и делаем push
git rm file_name - удал€ет файл - тоже самое


ll - все содержимое где сейчас находишьс€ или dir
cd .. - назад из папки 
cd названиеѕапки - заходит в папку

-- заливаем на гитхаб через консоль --
git init
git add --all
git commit -a -m 'first commit'
git remote add origin https://github.com/user/project.git !! помен€ть https на свой линк на репозиторий
git pull --rebase origin master
git push origin master
//если сменилс€ url то git remote rm origin а потом git remote add origin и ссылку на новый
-- откат --
git log - просмотреть историю коммитов
git checkout (и id коммита который хотим просмотреть) 6a28d66 - дл€ просмотра
git checkout master - вернутьс€ обратно
git revert (id коммита который нужно отменить) 6a28d66 - отмена коммита не удаление
:wq - выйти из редактора
git reset (id коммита который нужно отменить) 6a28d66 --hard - удал€ет коммиты с откатом файлов

//удаление локальных файлов и востановление
git rm названиефайла.txt -> далее коммит и пуш
git reset --hard -> если до комита и пуша, то вернет файл обратно
git checkout названиефайла.txt -вернет файл если после пуша

„тобы восстановить файл из конкретного коммита или ветки, можно сделать вот так:

git checkout abcde myfile # abcde Ч хеш коммита
# или
git checkout feature3 myfile # feature3 Ч им€ ветки
